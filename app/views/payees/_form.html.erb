<%= form_with(model: payee, local: true) do |form| %>
  <% if payee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payee.errors.count, "error") %> prohibited this payee from being saved:</h2>

      <ul>
      <% payee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :payee_user_id %>
  </div>

  <div class="field">
    <%= form.label :expense_id %>
    <%= form.text_field :expense_id, id: :payee_expense_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
