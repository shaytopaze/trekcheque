<%= form_with(model: @trip, local: true) do |form| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<% if !@trip.started %>
  <div class="field">
    <%= form.label :started %>
    <%= form.check_box :started, id: :started_id %>
  </div>
<% end %>

  <div class="field">
    <%= form.label :ended %>
    <%= form.check_box :ended, id: :ended_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>